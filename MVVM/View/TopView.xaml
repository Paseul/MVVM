<UserControl x:Class="MVVM.View.TopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVM"
             xmlns:realTimeGraphX="clr-namespace:RealTimeGraphX.WPF;assembly=RealTimeGraphX.WPF"
             mc:Ignorable="d" 
             DataContext="{Binding Source={StaticResource Locator}, Path=Top}"
             d:DesignHeight="300" d:DesignWidth="1520">
    <Grid>
        <local:WpfGraphControl Margin="10,10,420,0" Controller="{Binding MultiController}" VerticalAlignment="Top" Height="280"/>
        <ListBox HorizontalAlignment="Left" Height="280" Margin="1090,10,0,0" VerticalAlignment="Top" Width="410"/>
    </Grid>
</UserControl>
